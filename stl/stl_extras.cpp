#include <bits/stdc++.h>
using namespace std;

void explainExtras(){
    // sorting vectors and arrays
    vector<int> v;
    int Arr[6] = {1,4,2,9,1,41};
    v.emplace_back(1);
    v.emplace_back(4);
    v.emplace_back(2);
    v.emplace_back(144); // vector now is {1,4,2,144} 
    (0, 6); // for arrays { 1,1,2,4,9,41}
    sort(v.begin(), v.end()); //[start, end), now array is {1,2,4,144}

    sort(a, a+n; greatest<int>);    //descending order sort

}





int main(){
    explainExtras();
    return 0;

}