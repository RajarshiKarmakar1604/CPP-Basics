#include <bits/stdc++.h>
using namespace std;

void explainMS(){
   unordered_set<int>st;
   //unique values, but random order
   //lower_bound and upper_bound funciton does not work, rest all are same
   //it has a better complexity than set in most cases, except some when collission happens
   //most of the cases, time complexity is O(1) to O(n);   
   
}





int main(){
    explainMS();
    
    return 0;
}

